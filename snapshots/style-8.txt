STYLE 8 — SWISS / NUMBERED LIST (TWO COLUMNS)
Path: /8

=== TEXT REPRESENTATION OF DISPLAY ===

- Background: Light cream (#faf8f5)
- Typography: Fraunces (headings, buttons — serif), Open Sans (body)
- Accent: Deep red (#b71c1c) for numerals, links, primary button
- Text: #1a1a1a, muted #666

HEADER:
  Thin bottom border (2px) black. Left: "Procurement Links" (Fraunces), subtitle "State and city procurement portals" (gray). Right: "Import JSON" (outline black), "Add link" (solid red). Buttons rounded-md (not rounded-full, not rounded-lg).

MAIN CONTENT:
  Two-column layout (grid 2 cols on md): each column is a vertical numbered list. Each row: large numeral (1, 2, 3…) in red (Fraunces, right-aligned), then city (Fraunces), state, "Official site" / "Procurement portal" (red), "Edit". Rows have divide-y; hover row bg lightens. Not a card grid; numbered rows in two columns.

EMPTY STATE:
  Dashed gray border, rounded-md, "Add link" red outline button.

No purple/gold/teal/magenta/green. No single-column list only. No card grid. Numbered rows in two columns. Swiss/light tone. Fraunces + Open Sans. Red accent on light.
